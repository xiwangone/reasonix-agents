# Reasonix Agents — 对话交互设计（按 RikkaHub Agents 逻辑）

> 日期：2026-08-06 · 蓝本：RikkaHub Agents 对话交互逻辑（15 条核心模式，已调研）/tmp/rikkahub-agents
> 关联：`ui-design-direction-20260806.md`（run 模式 UI 方向，推荐 B「双面板工作室」）——本设计聚焦**对话交互层**，两者互补：方向 B 定"对话/Run/结果"三 Tab 骨架，本文定对话 Tab 内的交互细节。

---

## 一、设计原则

1. **服务端驱动为前提**：Reasonix 是 SSE 连 DeepSeek-Reasonix 的客户端，交互受协议能力约束——凡依赖服务端能力的项先 spike 验证协议，再做 UI。
2. **RikkaHub 为蓝本、非照搬**：取其交互范式（三态发送钮、思维链卡片、操作行、审批四按钮），裁剪掉依赖 RikkaHub 特有能力的项（80+ 设备工具、TTS 本地引擎等）。
3. **移动优先**：拇指可达、单手持握；沿用现有 Material 3 风格（LocalPalette）。

---

## 二、差距矩阵（RikkaHub 15 条 × Reasonix 现状）

图例：✅ 已有（可对齐/微调）· 🔶 部分（需补全）· ❌ 缺失（需新建）

| # | RikkaHub 交互模式 | Reasonix 现状 | 差距 | 落地建议 |
|---|---|---|---|---|
| 1 | 抽屉/侧栏双形态 + 顶部栏（重命名/预览/新建） | 悬浮侧边栏 220dp（新建/删除/批量删，ChatScreen.kt:1015/438）| 🔶 | 侧栏已有；补：顶部栏标题点击重命名、会话列表按日期分组 |
| 2 | 底部输入卡（附件行+多行+工具横条+右侧按钮） | Footer 输入区 + SlashMenu + 键盘快捷键 | 🔶 | 保持；补：工具横条（模型/推理强度）、全屏编辑器入口 |
| 3 | 发送钮三态 + 长按=只记录不生成 | 发送/停止切换（待核实） | 🔶 | 对齐三态样式（primary↑/禁用/红色停止）；补长按"只记录不生成"（须服务端支持空生成） |
| 4 | thinking+tool 组合为思维链步骤卡 | 独立 ReasoningBlock + ToolCard 并列 | 🔶 | 按 toolCallId 分组，渲染为链式步骤卡（含 Pending 审批强制展开） |
| 5 | 推理卡三态折叠（预览渐隐/展开/收起）+ 生成中 shimmer | ReasoningBlock（生成中展开） | 🔶 | 补三态折叠 + 时长 shimmer + 完成后按设置自动收起 |
| 6 | 工具统一 ToolUIRegistry + 审批四按钮 + ask_user 内联表单 | ToolCard + ApprovalCard + AskCard | 🔶 | 协议已支持 approve(id, allow, session, persist, scope)（ChatRepository.kt:95-98）→ UI 对齐四按钮（允许/∞/本聊天/拒绝+理由） |
| 7 | 自研 AST Markdown + 代码高亮 + mermaid + 公式 | Markwon v4（MarkdownRenderer.kt:60-103，自动换行 + 横滚）| ✅ | 保持；可选补 mermaid/公式（依赖库） |
| 8 | 消息下常驻操作行（复制/重生成/TTS/翻译/更多） | 无 | ❌ | **P0 新建**：复制/重生成/删除/更多；TTS/翻译置灰（服务端不支持则隐藏） |
| 9 | 点用户气泡=编辑态 | 无 | ❌ | 依赖服务端编辑协议（spike：ReasonixApi 是否支持消息编辑）；不支持则隐藏 |
| 10 | 分支导航（←/→ 1/N）+ Fork 新会话 | RewindPickerDialog（回退） | 🔶 | 保留 Rewind；分支需 spike 服务端（rewind 已支持，分支未必） |
| 11 | 会话抽屉=文件夹筛选+日期分组+长按菜单 | 侧栏（新建/删除/批量删） | 🔶 | 补：日期分组头、置顶、重生成标题（依赖服务端会话标题接口） |
| 12 | 附件面板聚合（拍照/图片/视频/文件+工作区+MCP+压缩设置） | 仅图片发送（本地 OCR） | ❌ | **P1**：附件 BottomSheet（图片已有，拍照/文件可本地实现；工作区/MCP 依赖服务端工具） |
| 13 | 自动压缩两级交互（触发点确认弹窗+设置） | CompactionNotice（服务端压缩通知） | 🔶 | 客户端触发压缩需 spike（服务端是否暴露 compress 接口）；先对齐通知展示 |
| 14 | token 统计双处（输入框下累计条+消息 NerdLine） | UsageStats + StatsDialog | 🔶 | 补输入框下会话累计条（↑输入/缓存/↓输出，可复制） |
| 15 | 无滑动删除、音量键滚动、多选导出、自动任务、收藏 | 键盘快捷键、Rewind、SlashMenu(/new) | 🔶 | 可选：消息多选导出（复用 SessionExporter）；自动任务/收藏依赖服务端，暂缓 |

---

## 三、分优先级路线图

### P0 — 对话基础体验（1-2 周）
1. **消息操作行**（#8）：复制 / 重生成 / 删除 / 更多（分享/导出）；生成中隐藏
2. **思维链步骤卡**（#4）：ReasoningBlock + ToolCard 按 toolCallId 合并为链式卡
3. **推理三态折叠**（#5）：Collapsed/Preview/Expanded + shimmer 时长
4. **审批四按钮对齐**（#6）：允许 / 始终允许 / 本聊天 / 拒绝+理由（协议已支持）
5. **token 累计条**（#14）：输入框下会话级统计，可复制

### P1 — 贴近 RikkaHub 完整度（2-3 周）
6. **附件面板**（#12）：BottomSheet 聚合图片/拍照/文件 + 压缩/工具设置入口
7. **会话抽屉增强**（#11）：日期分组 + 置顶 + 长按菜单
8. **发送钮三态 + 长按只记录**（#3）：样式统一 + 交互补全
9. **输入工具横条**（#2）：模型快捷切换 + 推理强度（对应 RikkaHub ReasoningButton，仅支持模型显示）

### P2 — 增强与差异化（按需）
10. 分支导航/Fork（#10，spike 服务端后定）
11. 消息编辑（#9，spike 服务端后定）
12. 消息多选导出（#15，复用 SessionExporter）
13. 折叠屏/平板：NavigationRail + 双栏（对接 run 模式方向 B）

---

## 四、关键设计细化（P0 项）

### 4.1 消息操作行
```
┌──────────────────────────────┐
│ 消息内容                      │
│ ──────────────────────────── │
│ 📋复制  🔄重生成  🗑删除  ⋯更多 │  ← 生成完成后显示；loading 隐藏
└──────────────────────────────┘
「更多」BottomSheet：分享/导出、WebView 渲染（可选）
```
- 实现：ChatItem 扩展 actionRow，挂 ChatMessage.kt 同级组件；删除带确认（对齐 RikkaHub ChatVM.kt:344-356 生成中禁止删除）

### 4.2 思维链步骤卡
- 依据：历史消息已有 toolCallId 配对逻辑（ChatViewModel buildHistoryItems）
- 渲染：`ReasoningBlock → 其调用的 ToolCard 们` 组合为一张链式卡；Pending 审批强制展开；loading 显示 DotLoading

### 4.3 审批四按钮
- 协议：`approve(id, allow, session, persist, scope)` 已支持（ChatRepository.kt:95-98）
- UI：ApprovalCard 内 4 按钮行：✅允许 / ♾️ 始终允许 / 💬 本聊天允许 / ❌ 拒绝（+理由输入）
- 对齐 RikkaHub ChatMessageTools.kt:156-338 的形态，去掉 RikkaHub 特有的 schedule_job/MCP 安全提示（Reasonix 无此工具集）

---

## 五、风险与前置 spike

| 项 | 风险 | spike 方法 |
|---|---|---|
| 消息编辑/重生成/分支 | 服务端协议可能不支持 | 读 ReasonixApi.kt + DeepSeek-Reasonix 协议（esengine/DeepSeek-Reasonix main-v2）确认端点 |
| 客户端触发自动压缩 | 服务端是否暴露 compress 接口未知 | 同上 |
| 长按"只记录不生成" | 需空生成/仅存储语义 | 同上 |
| 附件（文件/拍照） | 服务端工具集是否接收 | 现有图片通道已通（本地 OCR），扩展按同通道 |
| TTS/翻译 | 服务端无此能力 | P0 不包含，操作行置灰/隐藏 |

---

## 六、与 run 模式方向的关系

- **方向 B「双面板工作室」**（ui-design-direction 推荐）：定对话/Run/结果三 Tab 骨架
- **本设计**：填充对话 Tab 的交互细节（P0 项与方向 B 无冲突，均可在现有 ChatScreen 内增量落地）
- 落地顺序建议：先 P0 对话交互（本设计）→ 再 run 模式三 Tab 重构（方向 B）→ 平板 NavigationRail

---
*参考：RikkaHub 15 条模式来源 /tmp/rikkahub-agents（ChatPage/ChatInput/ChatMessage/ChatDrawer/ChatVM 等，见探索记录）*

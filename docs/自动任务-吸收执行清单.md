# 自动任务 · RikkaHub Agents 吸收执行清单

> 生成：2026-08-13 · 关联：《对比分析-RikkaHub-Agents吸收规划-20260813.md》
> 用法：每项含【目标】【改动文件】【验收标准】；执行者按序处理，每项独立 commit，全部通过才收工。
> 原则：理念级借鉴、自行重写实现，不搬 AGPL 代码；改动前先读现状，避免重复实现已有能力。

---

## T1 记忆分层注入（P0-1）⭐ 第一版（本次执行）

【目标】MemoryStore 增加 tier 分层：core 常驻注入；conditional 按用户消息关键词匹配注入。
【改动文件】
- `app/src/main/java/com/reasonix/agents/data/MemoryStore.kt` — MemoryItem 加 tier 字段；activeMemoriesText 加 userText 参数做 conditional 匹配；标记解析支持 tier 指定
- `app/src/main/java/com/reasonix/agents/ui/viewmodel/ChatViewModel.kt` — 两处注入调用点传入当前用户消息
- `app/src/main/java/com/reasonix/agents/ui/screen/SettingsMemoryScreen.kt` — 条目显示 tier 徽标 + 添加时可选 tier
【验收标准】
- [ ] 旧数据兼容：无 tier 字段的记忆反序列化为 core，不崩溃
- [ ] core 记忆每轮注入（行为与现状一致）
- [ ] conditional 记忆仅在用户消息命中关键词时注入
- [ ] 注入仍受 MAX_INJECT_CHARS 上限约束
- [ ] 设置页可区分查看/添加 core 与 conditional

## T2 自动压缩 UX（P0-2）

【目标】客户端自动触发压缩策略：双模式（百分比阈值 / token 消耗累计）+ 确认弹窗 + 生成中不弹窗延后。
【改动文件】ChatViewModel.kt（触发点检测）、新增压缩确认对话框组件、设置页配置项
【验收标准】- [ ] 对话 token 达阈值时弹确认；生成中不打断，延后到对话结束

## T3 Vault 凭据安全（P0-3）

【目标】服务器凭据/密码区域指纹门禁 + 备份/日志输出掩码。
【改动文件】ServerConfigScreen.kt（biometric 门禁）、CredentialCrypto.kt（掩码工具）、BackupManager.kt
【验收标准】- [ ] 进入凭据编辑前指纹验证；备份文件不落明文

## T4 缓存命中优化（P1）

【目标】提示词注入顺序稳定（保前缀），评估 tool schema 排序。
【改动文件】ChatViewModel.kt（注入顺序固化）
【验收标准】- [ ] 相同会话连续请求注入前缀字节稳定

## T5 悬浮窗交互升级（P1）

【目标】CI 悬浮球常态小圆点 + 点击卡片动画展开收起。
【改动文件】CiMonitorService.kt
【验收标准】- [ ] 常态 18dp 圆点，点击后卡片动画展开

## T6 网络代理（P1）

【目标】客户端连接代理设置（SOCKS5/HTTP），用于直连海外 VPS serve。
【改动文件】ServerConfigStore.kt、OkHttp 构建处
【验收标准】- [ ] 可配置代理并生效

## T7 CI 流程细节（P1）

【目标】构建/配置缓存 + 缓存防堆积 + 统一 APK 命名。
【改动文件】.github/workflows/build.yml
【验收标准】- [ ] 依赖缓存 key 指纹化，构建缓存固定 key

---

## 执行记录

| 任务 | 状态 | commit | 日期 |
|---|---|---|---|
| T1 记忆分层注入 | ✅ 完成 | e614db9 | 2026-08-13 |
| T2 自动压缩 UX | 待执行 | - | - |
| T3 Vault 安全 | 待执行 | - | - |
| T4 缓存优化 | 待执行 | - | - |
| T5 悬浮窗 | 待执行 | - | - |
| T6 网络代理 | 待执行 | - | - |
| T7 CI 细节 | 待执行 | - | - |

## 签收记录（2026-08-13）

| 任务 | 状态 | 提交 | 说明 |
|---|---|---|---|
| T2 自动压缩 UX | ✅ 完成 | f968611 | 双模式触发（百分比/token 累计）+ 确认弹窗 + 生成中延后；设置-显示可配 |
| T3 Vault 凭据安全 | ✅ 第一版 | b550f5d | CredentialCrypto.mask 输出掩码；指纹门禁待评估（需 androidx.biometric 联网依赖） |
| T4 缓存命中优化 | ✅ 完成 | 4c28497 | 注入顺序统一为 buildInjectedInput（antiEcho→提示词→记忆→CLI→文本），保前缀稳定 |
| T5 悬浮窗交互 | ✅ 完成 | 4c28497 | 常态圆点 + 卡片 scale/淡入动画展开收起 |
| T6 网络代理 | ✅ 完成 | 74aa5a9 | SOCKS5/HTTP 代理，设置-网络可配，变更即重建连接 |
| T7 CI 细节 | ✅ 完成 | ab98f8a | push 不发版 + 缓存优化 + 手动 workflow_dispatch 发版 |
